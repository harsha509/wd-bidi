// IModule.ts: This interface could be used to define the structure
// and functionality that all of your modules must follow.
// For example, if every module needs an initialize and a terminate function,
// you would include those function signatures in this interface.
//
// typescript
//
//
// export interface IModule {
//     initialize(): Promise<void>;
//     terminate(): void;
//     // and other methods that every module should have...
// }